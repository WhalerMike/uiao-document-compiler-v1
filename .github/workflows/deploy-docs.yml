# DISABLED: Redundant workflow - generate-docs.yml is the primary pipeline
# deploy-docs.yml used mkdocs gh-deploy which conflicts with GitHub Actions Pages source
# This caused duplicate github-pages artifact uploads (Pages build #370 failure)
name: Deploy Modernization Atlas (DISABLED)

on:
  workflow_dispatch:  # Manual trigger only - no longer auto-runs

jobs:
  deploy:
    if: false  # Disabled - use generate-docs.yml instead
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled"
